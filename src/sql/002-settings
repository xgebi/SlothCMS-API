CREATE TABLE settings (
    settings_name       VARCHAR(40) PRIMARY KEY,
    display_name        VARCHAR(100),
    settings_value      VARCHAR(255),
    section_id          VARCHAR(40)
);

ALTER TABLE settings ADD COLUMN setting_type VARCHAR(100) DEFAULT "core";
